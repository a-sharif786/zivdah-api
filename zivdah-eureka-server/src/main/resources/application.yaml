server:
  port: 8761

spring:
  application:
    name: zivdah-eureka-server

  kafka:
    bootstrap-servers: localhost:9092

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
  server:
    enable-self-preservation: false
