spring:
  application:
    name: zivdah-chat-service
  profiles:
    active: dev
  webflux:
    multipart:
      max-in-memory-size: 20MB
      max-disk-usage-per-part: 20MB
  codec:
    max-in-memory-size: 20MB

# Same shared Cloudinary account zivdah-product-service already uses (see CloudinaryUploadService
# in zivdah-common) — chat attachments land in their own folder, not mixed with product images.
cloudinary:
  cloud-name: ${CLOUDINARY_CLOUD_NAME:gvzwkqlf}
  api-key: ${CLOUDINARY_API_KEY:818952935514721}
  api-secret: ${CLOUDINARY_API_SECRET:lTfdXwdNr70TTFPFNs06gSteZeE}
  folder: ${CLOUDINARY_CHAT_FOLDER:zivdahonlinegrocery/chat-attachments}
